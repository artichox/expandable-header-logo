# expandable-header-logo
Header that expands towards the center of the screen on mouseover. Node + Famous

* Clone the repository
* run **npm install**
* run **grunt**
* Go to your browser at localhost:3000
